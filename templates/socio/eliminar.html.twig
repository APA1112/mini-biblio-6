{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Eliminar socio</h1>
    <div class="alert alert-danger">
        El socio {{ socio.nombre }} {{ socio.apellidos}} será eliminado de la aplicación.
        La operación no se puede deshacer.
    </div>
    <form method="post">
        <a href="{{ path('socio_listar') }}" class="btn btn-info">Volver al listado</a>
        <button type="submit" name="confirmar" class="btn btn-danger">Confirmar eliminación</button>
    </form>
{% endblock %}